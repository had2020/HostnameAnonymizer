# NetworkImposter

This Rust tool Randomly generates hostnames, and sets them on Linux with the sudo hostname command.

Every x mins a new name will be generated. It will be 1 and 50 vaild chars.

You can use this to Anonymize your machine a bit more ontop of Mac address randomiztion.
